# Завод Кёнел продаётся

Одностраничный адаптивный сайт о продаже действующего производства фасадных комплектующих.

## Локальный запуск

```powershell
python -m http.server 4173
```

После запуска открыть `http://localhost:4173/`.

Телефон для связи задаётся константой `PHONE` в `site.js`.
